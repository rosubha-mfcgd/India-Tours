# Travel Frontend (Material UI)

This frontend is a scaffold for the Tours API. It includes:
- Material UI layout
- Tours CRUD
- Itinerary builder
- Tour manager CRUD
- Mock payment flow
- JWT auth (uses token stored in localStorage)

Run:
1. npm install
2. npm start

API base URL is configured in src/api/axiosClient.js
