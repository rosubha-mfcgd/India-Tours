# Backend dependencies
npm init -y</br>
npm install express mongoose bcryptjs jsonwebtoken cors body-parser dotenv razorpay uuid</br>
npm install --save-dev typescript ts-node-dev @types/express @types/node @types/jsonwebtoken @types/cors @types/bcryptjs @types/uuid</br>
npx tsc --init</br>
npm i --save-dev @types/swagger-ui-express<br/>
npm install zod swagger-ui-express swagger-jsdoc</br>
npm install --save-dev ts-node-dev tsconfig-paths</br>

node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"<br/>

